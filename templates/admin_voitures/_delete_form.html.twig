<form method="post" action="{{ path('app_admin_voitures_delete', {'id': voiture.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir supprimer cette voiture ?');" class="delete-form">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voiture.id) }}">
    <button type="submit" class="btn btn-danger">
        <i class="fas fa-trash-alt"></i> Supprimer
    </button>
</form>
