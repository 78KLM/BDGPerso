<form method="post" action="{{ path('app_admin_evenement_delete', {'id': evenement.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir supprimer cet événement ?');" class="delete-form">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement.id) }}">
    <button type="submit" class="btn btn-danger">
        <i class="fas fa-trash-alt"></i> Supprimer
    </button>
</form>
